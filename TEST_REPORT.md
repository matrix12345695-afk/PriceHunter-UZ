# Проверка 1.4 — 03.09.2026

`python -m pytest tests -q`: **46 passed**, 3.29 s.

Проверены существующие API-парсеры, фильтры, интерфейс, фотографии (URL/JPEG/отказ),
webhook, очередь, healthcheck; новые парсеры на сохранённых реальных DOM-карточках:

| Источник | Карточки в fixture | Полная цена первой карточки |
|---|---:|---:|
| OLX | 4 | 15 933 915 UZS |
| Uzum | 4 | 17 999 000 UZS |
| Wildberries UZ | 4 | 17 461 700 UZS |

Проверены фото каждого товара, skuId Uzum, состояние/локация OLX, валюта, выбор
актуальной цены вместо зачёркнутой или ежемесячной. Проверены маршрутизация в браузер,
резерв Asaxiy после HTTP-отказа, кэш, закрытие контекста после успеха и 403,
отдельный статус отсутствующего Chromium. Браузерный драйвер проверен с mock Playwright.

Отдельная живая проверка обычным облачным браузером: каталоги Asaxiy, Uzum,
Wildberries и OLX открылись с названиями, фотографиями и ценами. Общий поиск OLX
/list/q-iphone-17-pro-max/ подтвердил режим «Любая категория» и показал объявления.
Это не запуск нового Python-драйвера на Render: новый Docker-образ здесь не собирался,
headless Chromium в окружении пользователя не проверен. Доставка Telegram-фото,
реальная PostgreSQL и end-to-end деплой не проверены из-за отсутствия доступа.

Elmakon: 502 и ошибка проверки имени сертификата; TLS-проверка не отключалась.
AliExpress: браузерная навигация отклонена политикой безопасности среды; автоматический
адаптер не реализован. Эти два источника остаются незавершёнными.

Старый diagnostics-v1.3.json — историческая проверка до добавления Chromium.
Актуальная диагностика после деплоя: `python -m pricehunter.diagnose "Iphone 17 pro max"`.

Установка браузера сверена с официальной документацией:
https://playwright.dev/python/docs/docker#build-your-own-image
