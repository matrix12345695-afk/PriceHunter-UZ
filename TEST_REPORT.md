# Проверка Render-обновления 1.1

- Сохранены совместимые точки входа python run.py, python -m app.main, app.web.server:app.
- Возвращён FastAPI webhook /webhook и /health; PostgreSQL через существующую DATABASE_URL.
- Новые таблицы ph_* создаются отдельно, старые пользовательские таблицы не удаляются.
- Тесты старого поиска адаптированы к асинхронному хранилищу.
- Добавлены проверки секретного заголовка, неправильного JSON, предела размера запроса,
  постановки в очередь перед HTTP 200, проверки базы в /health и успешной обработки очереди.
- PostgreSQL SQL проверен чтением; интеграционного теста с настоящим сервером PostgreSQL не было.
- Нет доступа к текущему GitHub/Render, вашей базе и Telegram-токену; реальный деплой не выполнялся.
- Публичные адаптеры Olcha, Mediapark, Asaxiy проверены в предыдущей сборке; исходные тесты сохранены.

Результат: 22 теста прошли, Python 3.12.
