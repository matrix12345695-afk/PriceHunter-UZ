from app.parsers.base_browser import BaseBrowserParser


class OlchaBrowserParser(BaseBrowserParser):

    async def search(self, query: str):

        raise NotImplementedError(
            "Research in progress."
        )
